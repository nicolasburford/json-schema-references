{
  "name": "json-schema-nav",
  "displayName": "JSON Schema References",
  "description": "Navigate local JSON Schema $ref references with hover previews and go to definition.",
  "version": "0.0.2",
  "publisher": "nicolasburford",
  "icon": "assets/icon.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nicolasburford/json-schema-references.git"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:json",
    "onLanguage:jsonc"
  ],
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "jsonc-parser": "^3.2.0"
  }
}
